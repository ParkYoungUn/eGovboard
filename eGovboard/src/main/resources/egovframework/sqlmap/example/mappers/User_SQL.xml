<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="egovframework.example.user.impl.UserMapper">

	<resultMap id="TB_USER" type="egovframework.example.user.service.UserVO">
		<result property="userId" column="USER_ID"/>
		<result property="loginId" column="LOGIN_ID"/>
		<result property="userName" column="USER_NAME"/>
		<result property="userNick" column="USER_NICK"/>
		<result property="userEmail" column="USER_EMAIL"/>
	</resultMap>

	<!-- 회원가입 -->
	<insert id="register">
	    INSERT INTO MP_MEMBER(  USER_ID 
	   					       ,LOGIN_ID
	   					       ,USER_NAME
	   					       ,USER_NICK
	   					       ,USER_EMAIL 	)
	                  VALUES(    #{userId} 
	                 		   , #{loginId}
	                 		   , #{userName}
	                 		   , #{userNick}
	                 		   , #{userEmail})
    </insert>

	<!-- 로그인 -->
	<select id="login" resultType="egovframework.example.user.service.UserVO">
		SELECT LOGIN_ID
		  FROM TB_USER 
		 WHERE USER_ID = #{userId}
		   AND LOGIN_ID = #{loginId}
	</select> 
	
</mapper>